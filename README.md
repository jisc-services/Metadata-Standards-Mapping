# Setup

    git clone git@github.com:drtjmb/jisc-mapping.git build
    cd build
    git checkout gh-pages
    cd ..
    vagrant up
    vagrant ssh
    cd /vagrant
    ./build.sh
    logout
    ./deploy.sh
